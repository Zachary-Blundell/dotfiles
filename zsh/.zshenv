# Keep the only zsh dotfile in $HOME minimal.
# ZDOTDIR tells zsh to look in ~/.config/zsh for the rest of its startup files
# such as .zshenv, .zprofile, .zshrc, .zlogin, and .zlogout.
export ZDOTDIR="$HOME/.config/zsh"
